#ifndef THREADPOOL_H
#define THREADPOOL_H

#include <thread>
#include <vector>
#include <functional>

namespace bigFileSort {
/*
class ThreadPool
{
    std::vector<std::thread> vThreads;

public:
    ThreadPool(size_t size, std::functional<void()> &&threadFunc);
};*/
}
#endif // THREADPOOL_H
