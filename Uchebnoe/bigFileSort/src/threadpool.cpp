#include "threadpool.h"

namespace bigFileSort {
/*
ThreadPool::ThreadPool(size_t size, std::functional<void()> &&threadFunc)
{
    vThreads.resize(size, threadFunc);
    for(int i = 0; i < vThreads.size(); ++i){
        vThreads[i].detach();
    }
}*/

}
